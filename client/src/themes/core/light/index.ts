import './index.scss';

import type {
    ITheme
} from '../../../interfaces/themes';

export default {
    id: 'light',
    name: 'Light',
    colour: '#FFFFFF',
    class: 'theme--light',
    mapStyle: 'light'
} as ITheme;